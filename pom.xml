<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <properties>
        <ebean.groupid>org.avaje.ebeanorm</ebean.groupid>
        <ebean.artifactid>avaje-ebeanorm</ebean.artifactid>
        <ebean.version>4.7.1-SNAPSHOT</ebean.version>
        <agentloader.groupid>org.avaje</agentloader.groupid>
        <agentloader.artifactid>avaje-agentloader</agentloader.artifactid>
        <agentloader.version>1.1.2</agentloader.version>
        <ebeanagent.groupid>org.avaje.ebeanorm</ebeanagent.groupid>
        <ebeanagent.artifactid>avaje-ebeanorm-agent</ebeanagent.artifactid>
        <ebeanagent.version>4.5.3</ebeanagent.version>
    </properties>

    <groupId>${ebean.groupid}</groupId>
    <artifactId>${ebean.artifactid}-osgi</artifactId>
    <version>1.2.0</version>
    <packaging>bundle</packaging>

    <dependencies>
        <dependency>
            <groupId>${ebean.groupid}</groupId>
            <artifactId>${ebean.artifactid}</artifactId>
            <version>${ebean.version}</version>
        </dependency>
        <dependency>
            <groupId>${agentloader.groupid}</groupId>
            <artifactId>${agentloader.artifactid}</artifactId>
            <version>${agentloader.version}</version>
        </dependency>
        <dependency>
            <groupId>${ebeanagent.groupid}</groupId>
            <artifactId>${ebeanagent.artifactid}</artifactId>
            <version>${ebeanagent.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>1.7</source>
                    <target>1.7</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>2.3.7</version>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Bundle-Name>Ebean-ORM OSGi</Bundle-Name>
                        <Bundle-SymbolicName>org.avaje.ebeanorm.osgi</Bundle-SymbolicName>
                        <Export-Package>
                            com.avaje*,
                            org.avaje*
                        </Export-Package>
                        <DynamicImport-Package>*</DynamicImport-Package>
                        <Import-Package>
                            javax.persistence*,
                            *;resolution:=optional</Import-Package>
                        <_versionpolicy>[$(version;==;$(@)),$(version;+;$(@)))</_versionpolicy>
                        <_removeheaders>Include-Resource,Private-Package,Embed-Dependency</_removeheaders>
                    </instructions>
                    <unpackBundle>true</unpackBundle>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <artifactSet>
                                <includes>
                                    <include>${ebean.groupid}:${ebean.artifactid}</include>
                                    <include>${agentloader.groupid}:${agentloader.artifactid}</include>
                                    <include>${ebeanagent.groupid}:${ebeanagent.artifactid}</include>
                                </includes>
                            </artifactSet>
                            <promoteTransitiveDependencies>true</promoteTransitiveDependencies>
                            <createDependencyReducedPom>true</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <id>avaje-ebeanorm-osgi-repo</id>
            <url>http://ebeanorm-deploy-host:8082/artifactory/libs-release-local</url>
        </repository>
        <snapshotRepository>
            <id>avaje-ebeanorm-osgi-repo</id>
            <url>http://ebeanorm-deploy-host:8082/artifactory/libs-snapshot-local</url>
        </snapshotRepository>
    </distributionManagement>
</project>